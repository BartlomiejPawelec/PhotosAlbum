import React from 'react';
import Loader from 'react-loader-spinner'

const Loading = () => {
    return(
        <Loader 
            type="Rings"
            color="#000"
        />        
    )
}

export default Loading;